import { Confirmation,
  Warning
} from 'transactions-interface-web'

const view = { categoryName: 'modal',
  ComponentsByName: {
    ConfirmationModal: Confirmation,
    WarningModal: Warning
  }
}

export default view
